const test=require('node:test'),assert=require('node:assert/strict');const {request,answer}=require('../electron/questions.cjs');
test('native questions deliver an explicit user choice once and reject stale requests',()=>{const writes=[],events=[];const task={id:'task'},runner={active:{task,profile:{id:'codex'},rpc:{write:v=>writes.push(v)}},event:(...v)=>events.push(v)};assert.equal(request(runner,task,{id:12,params:{questions:[{id:'style',question:'Choose',options:[{label:'One'}]}]}}),true);assert.throws(()=>answer(runner,'task','12',{}));answer(runner,'task','12',{style:{answers:['One']}});assert.deepEqual(writes[0],{id:12,result:{answers:{style:{answers:['One']}}}});assert.equal(events[1][1],'user');assert.throws(()=>answer(runner,'task','12',{style:{answers:['Two']}}));});
